<%= form_with(model: @activity, url: new_activity_path,  data: {turbo: false}) do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>

  <%= form.label :description %>
  <%= form.text_area :description %>

  <%= form.submit "Enviar" %>
<% end %>